#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)

"$repo_root/tests/standalone-smoke.sh"
"$repo_root/tests/standalone-integration.sh"
"$repo_root/tests/conform-project-config.sh"
"$repo_root/tests/conform-toggle.sh"
"$repo_root/tests/lsp-attach-keymaps.sh"
"$repo_root/tests/lsp-keymap-conflicts.sh"
"$repo_root/tests/lsp-nonfile-detach.sh"
"$repo_root/tests/ufo-folding.sh"
"$repo_root/tests/theme-lazy-loading.sh"

"$repo_root/tests/normal-entrypoint.sh"
